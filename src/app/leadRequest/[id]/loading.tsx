import PageLoading from "src/components/LoadingComponents/PageLoading";

const Loading = () => {
  // You can add any UI inside Loading, including a Skeleton.
  return <PageLoading />;
};

export default Loading;
